<?php

/*
    |--------------------------------------------------------------------------
    | Development Version
    |--------------------------------------------------------------------------
    |
    | This value is the version of your application, which will be used when the
    | to define which operation to perform.
    |
    */
return [
    'dev_version' => (int) env('DEV_V', 1)
];

